{
  "name": "Sample",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "predeploy": "coffee -c app config",
    "deploy": "croquis_deploy",
    "postdeploy": "git clean -f app config > /dev/null"
  },
  "dependencies": {
    "@croquiscom/crary": "^0.5.8",
    "bluebird": "^2.9.15",
    "config": "^1.12.0",
    "lodash": "^3.6.0",
    "redis": "^0.12.1"
  },
  "devDependencies": {
    "coffeescript": "^2.0.3"
  }
}
